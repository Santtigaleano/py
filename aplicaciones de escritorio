'''
def boton_presionado():
    print("hola mundo")
import tkinter as tk #importo libreria para apl de escritorio
ventana= tk.Tk() #invoco a la ventana
ventana.config(width=600,height=400)##ancho y alto en pixeles
ventana.title("Bienvenido a la aplicación")
boton = tk.Button(text="aceptar",command=boton_presionado)
boton.place(x=20, y=150, width=100,height=40)
#ventana.mainloop() #comando para que cierre ventana

caja= tk.Entry()
caja.place(x=20, y=100, width=100, height=25)
etiqueta= tk.Label (text="Ingresa tu nombre", bg="#0ABAF3")
etiqueta.place(x=20, y=70)
ventana.mainloop() 
'''
'''
#place se usa para colocar la posición

import tkinter as tk


vebtaka1= tk.Tk()
ventana1.config(width=500, height=400) 
ventana1.tittle("Bienvenido escritorio")

def boton_presionado():
    print("hola mundo")
    print(caja.get()) #get toma valores dentro de caja de texto

boton= tk.Button(text="cliquea aquí", command=boton_presionado)
boton.place(x=20, y=40, width=100, height=25)


caja=tk.Entry()
caja.place(x=20, y=100, width=100, height=25)
etiqueta= k.Label (text="Ingresa tu nombre")
etiqueta.place(x=20, y=130)
ventana.mainloop()
'''

